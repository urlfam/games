#!/usr/bin/expect

set timeout 600
spawn ssh root@147.93.7.103 "cd /root/puzzio && docker stop puzzio-web-container || true && docker rm puzzio-web-container || true && docker-compose up -d web"
expect {
  "yes/no" { send "yes\r"; exp_continue }
  "password:" { send "Aissayoub21\r" }
}
expect eof

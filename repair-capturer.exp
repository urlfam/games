#!/usr/bin/expect

set timeout 600
spawn ssh root@147.93.7.103 "cd /root/puzzio && docker-compose stop image-description || true && docker-compose rm -f -s -v image-description || true && docker-compose up -d --force-recreate image-description"
expect {
  "yes/no" { send "yes\r"; exp_continue }
  "password:" { send "Aissayoub21\r" }
}
expect eof

#!/usr/bin/expect

set timeout 600

spawn ssh -o StrictHostKeyChecking=no root@147.93.7.103 "docker rm -f puzzio-web-container && cd /root/puzzio && docker-compose up -d web"
expect {
  "yes/no" { send "yes\r"; exp_continue }
  "password:" { send "Aissayoub21\r" }
}
expect eof
